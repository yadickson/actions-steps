name: 'actions-steps/project-build/python'
description: 'Building python project'

branding:
  icon: 'box'
  color: 'green'

runs:
  using: "composite"
  steps:

    - name: building python project
      if: hashFiles('pyproject.toml') != ''
      run: |
        source .venv/bin/activate
        poetry build
      shell: bash
