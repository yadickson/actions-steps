name: 'actions-steps/project-badge/version'
description: 'Create project badge version'
inputs:
  gist-id:
    description: gist id
    required: true

  gist-token:
    description: gist token
    required: true

branding:
  icon: 'box'
  color: 'green'

runs:
  using: "composite"
  steps:

    - uses: actions/download-artifact@v8
      with:
        name: version

    - if: hashFiles('version.txt') != ''
      run: echo "VERSION=$(cat version.txt)" >> $GITHUB_ENV
      shell: sh

    - run: echo "REPO_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_ENV
      shell: sh

    - if: hashFiles('version.txt') != ''
      uses: schneegans/dynamic-badges-action@v1.8.0
      with:
        gistID: ${{ inputs.gist-id }}
        auth: ${{ inputs.gist-token }}
        filename: ${{ env.REPO_NAME }}-version.json
        label: version
        message: ${{ env.VERSION }}
        color: brightgreen
        forceUpdate: true
        style: flat
